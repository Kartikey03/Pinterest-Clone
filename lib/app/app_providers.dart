/// Top-level Riverpod providers.
///
/// This file holds app-wide providers that don't belong
/// to any specific feature module (e.g., theme mode, connectivity).
/// Feature-specific providers live in their respective feature folders.
library;

// Theme mode provider — controls light/dark mode switching
// Will be expanded in future modules.
